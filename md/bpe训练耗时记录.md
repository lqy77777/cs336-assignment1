# BPE 训练耗时记录

记录时间：2026-09-11T14:52:09+08:00

## 运行结果

训练成功完成，总墙钟耗时 **119.246 秒（约 1.99 分钟）**。返回 **10,000 个词表项、9,743 条合并规则**。

- 代码：`cs336_basics/bpe.py`。
- 输入：`data/TinyStoriesV2-GPT4-train.txt`，2,227,753,162 字节，约 2.23 GB。
- 特殊 token：`["<|endoftext|>"]`；工作进程：6；目标词表大小：10,000。
- Python：3.13.14；系统：macOS-26.5.2-arm64-arm-64bit-Mach-O。
- CPU：10 个物理核心、10 个逻辑核心；物理内存：16.0 GiB。

## 各阶段耗时

| 阶段 | 墙钟耗时（秒） | 占总时间 |
| --- | ---: | ---: |
| 初始化词表 | 0.000033 | 0.00% |
| 查找分块边界 | 0.000306 | 0.00% |
| 构造任务与启动进程池 | 0.010673 | 0.01% |
| 并行读取、预分词与局部计数（含结果传输） | 92.724098 | 77.76% |
| 汇总局部计数与关闭进程池 | 0.044365 | 0.04% |
| 构建初始pair计数和倒排索引 | 0.104528 | 0.09% |
| BPE合并循环 | 26.349016 | 22.10% |

阶段之和与总耗时之间的少量差异来自测量设置和阶段日志等开销。

## 合并阶段拆分

| 操作 | 耗时（秒） | 占合并阶段 |
| --- | ---: | ---: |
| 累计执行 `max(pairs, ...)` 选择最高频 pair | 24.978735 | 94.80% |
| 其余合并、计数及索引更新、进度输出和测量开销（差值） | 1.370282 | 5.20% |

本次预分词相关阶段占总时间约 77.8%，是最耗时的阶段；合并阶段中约 94.8% 用于扫描候选并选择最高频 pair。后续可分别关注预分词吞吐量和最高频 pair 的查找成本。

## 计时口径与限制

- 实际运行全量语料，真实启动 6 个工作进程。通过命令在内存中加入 `time.perf_counter()` 时间标记，未修改或新增 `.py` 文件。
- 总耗时覆盖训练调用及少量测量设置，不包含 Python 解释器启动、模块首次导入和最终报告写入。
- 预分词阶段记录主进程等待 `starmap()` 的墙钟时间，包含工作进程就绪、读取与解码、特殊 token 分隔、正则预分词、编码、局部计数、结果序列化及传输。未单独测量这些子步骤。
- 进程池创建计时只覆盖主进程构造调用，子进程启动和导入的剩余等待可能计入预分词阶段。
- 原代码逐轮打印词表索引的行为保留，输出重定向到日志，其耗时包含在合并阶段。
- 本次是单次测量，没有串行对照，不能据此计算多进程加速倍数。操作系统缓存和其他应用负载可能影响复测结果。
- 运行约 92 秒时，观察到六个工作进程均在使用 CPU，系统可用内存约 5.29 GiB；不是峰值内存测量。
- 已确认训练完成及返回大小，未对全量语料训练结果另做正确性测试。词表和合并规则未保存到磁盘，本次保存的是耗时记录及日志。

## 原始记录

- [运行日志](bpe运行日志.txt)
- [精确计时 JSON](bpe阶段耗时.json)
- `inspect.getsource()` 获取的源代码文本 SHA-256：`2f83ca287c085177d59bbcce07bcf5ef130ff63ae7b34140c2bb0965a6cf80f1`。运行完成后以相同方式核对一致；该文本补齐末尾换行，与原始文件字节哈希口径不同。
